{
    "private": true,
    "type": "module",
    "module": "src/main.ts",
    "scripts": {
        "dev": "bun --watch run src/main.ts",
        "start": "bun .",
        "typecheck": "bun run tsc",
        "check-all": "bun run typecheck"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^6.0.3"
    }
}